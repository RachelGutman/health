import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.helthtest.app',
  appName: 'health-proj',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
